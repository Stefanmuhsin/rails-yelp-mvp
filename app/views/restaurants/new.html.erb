<h1>Add a new Restaurant</h1>
<%= simple_form_for(@restaurant) do |f| %>
<%= f.input :name %>
<%= f.input :address %>
<%= f.input :category, collection: Restaurant::CATEGORIES, prompt: "Select a category" %>
<%= f.input :phone_number %>
<%= f.submit %>
<% end %>
<%= link_to 'Back', restaurants_path %>
